@model IEnumerable<QCapp.Models.SchemaViewModel.DocumentConfigurationViewModel>

@{
    ViewData["Title"] = "Document List";
    Layout = "~/Views/Shared/_LayoutIn.cshtml";
}

<div class="table-responsive">
    <table class="table table-striped jambo_table bulk_action">
        <thead>
            <tr class="headings">
                <th>
                    <input type="checkbox" id="check-all" class="flat">
                </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.DocumentId) </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.DocumentSubId) </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.DocumentName) </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.ClientDocumentName) </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.Template) </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.Critical) </th>
                <th class="column-title">@Html.DisplayNameFor(model => model.Enable) </th>
                <th class="column-title no-link last">
                    <span class="nobr">Action</span>
                </th>
                <th class="bulk-actions" colspan="7">
                    <a class="antoo" style="color:#fff; font-weight:500;">Bulk Actions ( <span class="action-cnt"> </span> ) <i class="fa fa-chevron-down"></i></a>
                </th>
            </tr>
        </thead>

        <tbody>
            @{
                int rowNo = 0;
                foreach (var item in Model)
                {
                    <tr class="@(rowNo%2 == 0 ? "even" : "odd") pointer">
                        <td class="a-center "><input type="checkbox" class="flat" name="table_records"></td>
                        <td>@item.DocumentId</td>
                        <td>@item.DocumentSubId</td>
                        <td>@item.DocumentName</td>
                        <td>@item.ClientDocumentName</td>
                        <td>@item.Template</td>
                        <td>@item.Critical</td>
                        <td>@item.Enable</td>
                        <td class=" last">
                            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                        </td>
                    </tr>

                    rowNo++;
                }
            }
        </tbody>
    </table>
</div>